{% extends "admin.html.twig"%}

{%block style %}
    {#Ici je vais rajouter les css qui s'appliquent uniquement à la page modifprofil   #}
{%endblock%}

{%block nav %}
    <li class="nav-item active">
        <a class="nav-link" href="{{path("admin")}}">{%if app.user.username is defined%}
            Bienvenue {{app.user.pseudo}}</a></li>
            {%endif%}
        <li class="nav-item">
            <a class="nav-link" href="{{path("add")}}">Ajouter une article</a></li>
        <li class="nav-item">
            <a class="nav-link" href="{{path("articles")}}">Voir les articles</a></li>

        <li class="nav-item">
            <a class="nav-link" href="{{path("profil")}}">Voir mon profil</a></li>
        <li class="nav-item">
            <a class="nav-link" href="{{path("brouillons")}}">Voir {%if app.user.roles[0] == "ROLE_ADMIN"%}les {%else%}mes {%endif%}brouillons</a></li>
        <li class="nav-item">
            <a class="nav-link" href="{{path("admin")}}">Retour page admin</a></li>
        <li class="nav-item">
            <a class="nav-link" href="{{path("accueil")}}">Retour sur le site</a></li>
            {%endblock%}

            {% block articles %}

                <h1 class="titradmin">Modifiez votre profil</h1>

                {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
                {{form(form)}}

                <a href="{{path("modifPass")}}"><input type="button" value="Modifier votre mot de Passe"></a>


                <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
                <script src="../script/verifprofil.js"></script>
            {%endblock%}
